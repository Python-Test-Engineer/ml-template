starting with:

`claude --dangerously-skip-permissions` bypasses all prompts to continue.

It can be dangerous if it goes rogue but for this work it can be OK.

/all command sets this level of permission as well.